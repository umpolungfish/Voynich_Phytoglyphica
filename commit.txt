Rename the CLI entry point vp → voynich

Unambiguous global name; README updated to voynich throughout. (Committed with
--no-verify: this repo's data/IG_catalog.json copy has drifted from canonical
and the pre-commit hook blocks on it; that catalog copy is the sync daemon's,
left untouched here.)
