Add 140 ARS entries, vp imscribe, ADDR gate, vp list/search, dosage output, VMS-primary README, demo

lang/voynich-engine/voynich_engine/session.py — ADDR gate (seven-gate protocol)
  - docstring was always "seven-gate" with ADDR listed but never implemented
  - added _gate_addr(): confirms d(plant, botanical) ≤ 1.5 before GATE1
  - SessionState gains addr_d_botanical and addr_passed fields
  - run() gains d_botanical parameter; ADDR runs after INIT when provided
  - report() shows Botanical d= [PASS/FAIL] in session header
  - all six VMS sections now active participants in every session run

voynich_phytoglyphica/elaborator.py — dosage_specification()
  - new function: derives specific serving volume, frequency, course duration,
    and daily total from structural tuple + d_astronomical
  - Ç → preparation form and base volume (tincture mL vs decoction mL vs drops)
  - potency tier (summa/alta/media/debilis) from d_astronomical → dose multiplier
  - Σ + Ω → frequency and course duration
  - Ħ=𐑫 (eternal chirality) → safety warning at lower end of range
  - tinctures: daily total ≡ g dried herb/day via 1:N ratio
  - decoctions/infusions: daily volume of prepared liquid only
  - essential oils: daily drops in carrier

voynich_phytoglyphica/cli.py — new commands + ADDR gate wiring + dosage
  - cmd_run() calls dosage_specification() after elaborated recipe output
  - cmd_list(): browse all 140 ARS phytoglyphica entries grouped by type (I–XI)
  - cmd_search(query): keyword search across plant name and description
  - passes d_botanical from navigator to session for ADDR gate
  - registered vp imscribe command

voynich_phytoglyphica/imscriber.py — dosage shown after VMS section distances
  - d_astronomical from section_distances()['astronomical'] feeds dosage_specification()

programs/ingest_ars_expanded.py — new ingestion script
  - 140 unique entries from ARS_PHYTOGLYPHICA_EXPANDED.md
  - catalog 3412 → 3552 entries

voynich_phytoglyphica/navigator.py — list_phytoglyphica(), section_distances()
  - list_phytoglyphica(query=None): filter by baseline, group by type label
  - section_distances(tuple_vals): in-memory tuple → distances to all 6 VMS sections

README.md — comprehensive documentation, VMS-primary framing
demo.sh — 8-step demonstration with ADDR gate and vp list/search/imscribe
