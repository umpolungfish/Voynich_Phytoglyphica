Add 140 ARS entries, vp imscribe, ADDR gate, VMS-primary README, demo

lang/voynich-engine/voynich_engine/session.py — ADDR gate (seven-gate protocol)
  - docstring was always "seven-gate" with ADDR listed but never implemented
  - added _gate_addr(): confirms d(plant, botanical) ≤ 1.5 before GATE1
  - SessionState gains addr_d_botanical and addr_passed fields
  - run() gains d_botanical parameter; ADDR runs after INIT when provided
  - report() shows Botanical d= [PASS/FAIL] in session header
  - all six VMS sections now active participants in every session run

voynich_phytoglyphica/cli.py — passes d_botanical from navigator to session
  - cmd_run() reads distances['botanical'] from nav.lookup() and forwards
    to sess.run(d_botanical=...) to engage the ADDR gate
  - registered vp imscribe command (imscriber.run_assessment())

programs/ingest_ars_expanded.py — new ingestion script
  - 140 unique entries from ARS_PHYTOGLYPHICA_EXPANDED.md
  - 14 cross-reference entries skipped
  - 11 structural types (I–XI) with hardcoded Shavian tuples
  - all 140 were new (catalog 3412 → 3552 entries)
  - global coverage: all 5 inhabited continents

voynich_phytoglyphica/imscriber.py — new interactive imscription wizard
  - 5 fixed baseline primitives shown, 7 discriminating primitives assessed
  - shows tuple, elaborated protocol, VMS section distances
  - optional catalog save and immediate session run

voynich_phytoglyphica/navigator.py — added section_distances(tuple_vals)
  - raw 12-element tuple → distances to all 6 VMS sections

README.md — comprehensive documentation, VMS-primary framing
  - VMS physical description (Beinecke MS 408, 1404–1438, six sections)
  - section-by-section description of what each actually shows
  - seven-gate session protocol (INIT ADDR GATE1 GATE2 GATE3 OUTPUT ELABORATION)
  - ADDR gate rationale: botanical illustrations as identity confirmation register
  - 11 structural type table framed as VMS botanical categories
  - self-report: how VMS illustrations encode pharmaceutical parameters
  - CLI reference with real VMS folio references (f11r, f103r, f69, f80v)

demo.sh — 8-step demonstration script covering five structural types,
  the full vp run pipeline (now showing ADDR gate), type contrast, vp imscribe
